# Manual evidence provenance

## This session (2026-08-09 / 2026-08-10)
Browser MCP (`cursor-ide-browser`) could not complete a live Studio walk:
tabs created (`browser_tabs` action `new`) returned viewIds then immediately became
"Browser view not found" / "No browser tab available" on `browser_navigate` and `browser_lock`.
Dev server on localhost:3000 responded HTTP 200.

No new live captures were possible in this session.

## Files here
Copied from prior QA folders for reference only (Phase B visual acceptance + one prior harness that *did* open Studio). Filenames note `__phase-b` where source is older acceptance, not this manual pass.

**Do not treat as product_matrix complete.** Contaminated prior harness outputs (identical Account-settings PNGs mislabeled as Midnight×Black Tie Live Preview) were **not** copied.
