/**
 * Vendor-app private notes — NOT venue Internal notes.
 *
 * event_vendor_assignments.internal_notes (and vendor-CRM inquiry notes)
 * belong to the vendor team only. They must never reach the venue workspace
 * or the couple portal.
 */
export const VENDOR_PRIVATE_NOTES_LABEL = "Internal notes";

export const VENDOR_PRIVATE_NOTES_HINT =
  "Private to your vendor team — never visible to the venue or client.";
