/**
 * Floor-plan notes are part of the floor-plan artifact — not venue-only
 * Internal notes. When a venue shares a floor plan with the couple, plan
 * (and object) notes travel with it via get_portal_floor_plan.
 */
export const FLOOR_PLAN_NOTES_LABEL = "Notes";

export const FLOOR_PLAN_NOTES_SHARE_HINT =
  "Notes on a shared floor plan may be visible to the client.";
