import { DEFAULT_ACTOR_ID } from "./seed";

/** Documented demo login for Project 8 (safe for client imports). */
export const DEMO_LOGIN = {
  email: "jennifer@hellotocheers.com",
  password: "cheers-demo",
  memberId: DEFAULT_ACTOR_ID,
} as const;
